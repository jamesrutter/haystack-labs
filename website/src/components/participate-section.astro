---
---

<section class="section section--accent">
  <div class="wrapper">
    <h2 class="section-title">How to Participate</h2>
    <div class="participate-content">
      <p class="participate-text">
        Haystack Labs is an invitational program. If you're interested in
        participating, please contact us at <a
          href="mailto:fablab@haystack-mtn.org"
          class="email-link">fablab@haystack-mtn.org</a
        >.
      </p>
    </div>
  </div>
</section>

<style>
  .section-title {
    font-family: var(--font-family-display);
    font-weight: var(--font-weight-bold);
    font-size: var(--font-size-3xl);
    text-align: center;
    margin-bottom: var(--space-3xl);
    color: var(--color-text-primary);
  }
  
  .participate-content {
    max-width: var(--container-md);
    margin-inline: auto;
    text-align: center;
  }
  
  .participate-text {
    font-size: var(--font-size-lg);
    line-height: var(--line-height-relaxed);
    color: inherit;
    margin: 0;
  }
  
  .email-link {
    color: var(--color-haystack-blue);
    text-decoration: none;
    font-weight: var(--font-weight-semibold);
    transition: var(--transition-normal);
  }
  
  .email-link:hover {
    color: var(--color-dark-teal);
    text-decoration: underline;
  }
</style>