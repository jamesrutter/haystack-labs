---
import { Image } from "astro:assets";
import Network from "@/assets/network.svg";
import HandShake from "@/assets/handshake.svg";
import Flask from "@/assets/flask.svg";
---

<section class="features">
  <div class="features__container">
    <div class="features__intro">
      <h2 class="features__title">EXPERIMENTAL CRAFT & TECHNOLOGY</h2>
      <p class="features__description">
        Haystack Labs brings together artists, designers, material scientists,
        machine builders, and coders to explore the integration of emerging
        technologies and traditional craft processes.
      </p>
    </div>

    <div class="features__grid">
      <div class="features__card">
        <div class="features__card-icon">
          <span class="features__icon"
            ><Flask width={48} height={48} fill="currentColor" /></span
          >
        </div>
        <h3 class="features__card-title">INTERDISCIPLINARY</h3>
        <p class="features__card-description">
          A unique blend of traditional craft disciplines with cutting-edge
          technology, fostering innovation through diverse perspectives.
        </p>
      </div>

      <div class="features__card">
        <div class="features__card-icon">
          <span class="features__icon"
            ><HandShake width={48} height={48} fill="currentColor" /></span
          >
        </div>
        <h3 class="features__card-title">COLLABORATIVE</h3>
        <p class="features__card-description">
          Intensive week-long charrette format encouraging rapid prototyping,
          experimentation, and knowledge sharing.
        </p>
      </div>

      <div class="features__card">
        <div class="features__card-icon">
          <span class="features__icon"
            ><Network width={48} height={48} fill="currentColor" /></span
          >
        </div>
        <h3 class="features__card-title">OPEN SOURCE</h3>
        <p class="features__card-description">
          All projects and knowledge are shared openly, contributing to a
          growing repository of craft-technology innovations.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .features {
    background-color: var(--color-background-secondary);
    padding: var(--space-5xl) 0;
  }

  .features__container {
    max-width: var(--container-7xl);
    margin-inline: auto;
    padding: 0 var(--space-lg);
  }

  .features__intro {
    text-align: center;
    max-width: 900px;
    margin-inline: auto;
    margin-block-end: var(--space-4xl);
  }

  .features__title {
    font-family: var(--font-family-display);
    font-weight: var(--font-weight-bold);
    font-size: var(--font-size-3xl);
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing-wide);
    margin-bottom: var(--space-xl);
    color: var(--color-text-primary);
  }

  .features__description {
    font-size: var(--font-size-lg);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-secondary);
    text-align: center;
    text-wrap: balance;
  }

  .features__grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-2xl);
    margin-top: var(--space-4xl);
  }

  .features__card {
    background-color: var(--color-background);
    padding: var(--space-2xl);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    text-align: center;
    transition: var(--transition-normal);
  }

  .features__card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .features__card-icon {
    width: 80px;
    height: 80px;
    margin: 0 auto var(--space-xl);
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(
      135deg,
      var(--color-primary),
      var(--color-secondary)
    );
    border-radius: var(--radius-full);
  }

  .features__icon {
    font-size: var(--font-size-2xl);
    color: var(--color-background);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .features__icon svg {
    display: block;
  }

  .features__card-title {
    font-family: var(--font-family-display);
    font-weight: var(--font-weight-bold);
    font-size: var(--font-size-lg);
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing-wide);
    margin-bottom: var(--space-md);
    color: var(--color-text-primary);
  }

  .features__card-description {
    font-size: var(--font-size-sm);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-secondary);
    margin: 0;
  }

  @media (max-width: 768px) {
    .features {
      padding: var(--space-3xl) 0;
    }

    .features__container {
      padding: 0 var(--space-md);
    }

    .features__intro {
      margin-bottom: var(--space-3xl);
    }

    .features__title {
      font-size: var(--font-size-2xl);
    }

    .features__description {
      font-size: var(--font-size-base);
    }

    .features__grid {
      grid-template-columns: 1fr;
      gap: var(--space-xl);
      margin-top: var(--space-3xl);
    }

    .features__card {
      padding: var(--space-2xl);
    }

    .features__card-icon {
      width: 60px;
      height: 60px;
      margin-bottom: var(--space-lg);
    }

    .features__icon {
      font-size: var(--font-size-xl);
    }
  }
</style>
